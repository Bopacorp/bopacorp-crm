# Phase 9 — Execution Record — 2026-08-15

## Result

- Date: 2026-08-15
- CRM revision at execution start: `493cababdce31623e5d174fd3bda6bbf2b9516db`
  (Phase 9 changes were uncommitted during the run)
- API revision: `4018bd58c6856e9258b7e79b0b91eeaa852a5459`
- Shared-package revision: `e539966b818d2142d171abc354c4ef1474c23d90`
- Seed revision: existing local seeded environment; not captured separately
- Environment: local development servers, test-only data
- CRM URL: `http://localhost:5173`
- API URL: `http://localhost:3000/api/v1`
- Browser: Chromium, Playwright `1.62.1`
- Workers: 1
- Command: `npm run test:e2e -- e2e/mutations-crm.spec.ts e2e/mutations-documents.spec.ts e2e/mutations-catalog.spec.ts e2e/mutations-employability.spec.ts e2e/mutations-reports.spec.ts`
- Result: **5 passed, 0 failed, 0 skipped**
- Cleanup: passed for all five journeys; no cleanup-error attachment was produced
- Document cleanup: logical record cleanup verified with 404; physical storage
  deletion is not claimed because the current API performs a soft-delete.

The complete browser command `npm run test:e2e` was also run with the same
local services and role variables. It passed **16 of 16 tests**, combining the
11-test Phase 8 read-only baseline with these five Phase 9 mutable journeys.

## Passed cases

- `E2E-CRM-09` — CRM client, negotiation, state, and visit journey — 38.1 s
- `E2E-CRM-10` — document approval/rejection/history journey — 32.6 s
- `E2E-CRM-11` — catalog product/image lifecycle — 27.9 s
- `E2E-CRM-12` — employability vacancy journey — 20.4 s
- `E2E-CRM-13` — sales-target edit/restore journey — 11.8 s

The complete run finished in approximately 2.2 minutes.

## Generated artifacts

Playwright wrote local ignored output to:

```text
playwright-report/
test-results/
```

The successful journeys produced the evidence screenshots named in the
[matrix](./02-mutable-journey-matrix.md). They remain local because the current
seeded UI can show real-looking names, emails, and business records. Review and
anonymize before copying any image into an academic report.

## Additional checks

- `npm run lint` passed; Biome checked 292 files.
- `npx biome check --write e2e` passed.
- `npm run test:e2e:typecheck` passed.
- `npm run test:run` passed: 174 tests in 47 files.
- `npm run build` passed; Vite emitted only the existing large-chunk warning.
- The Phase 8 read-only baseline had already passed 11/11 in its recorded local
  execution.

## Interpretation

This is illustrative mutable integration/E2E evidence, not a global browser
coverage claim. The unit/component and API integration suites remain the source
of coverage and contract breadth. CI promotion is still pending isolated
service startup, secret injection, artifact publication, anonymization review,
and a separate physical document storage cleanup contract.
