# Phase 8 — Initial Playwright Execution Record

## Result

- Date: 2026-08-15
- CRM SHA: `98db999` at execution start; the Phase 8 changes were uncommitted
  during this local run.
- API SHA: not captured in this run.
- Shared-package SHA: not captured in this run.
- Seed revision: existing local seeded environment; revision not captured.
- Environment: local development servers
- CRM URL: `http://localhost:5173`
- API URL: `http://localhost:3000/api/v1`
- Browser: Chromium only
- Command: `npm run test:e2e`
- Result: 11 passed, 0 failed, 0 skipped
- Mutations: none; the selected journeys only read data or cancel a review form.

## Covered cases

- Five role login, role-navigation, landing-route, and logout checks.
- Advisor client and negotiation read-only navigation.
- Advisor negotiation document/history visibility.
- Coordinator documentation action-menu and rejection-form inspection without
  submission.
- Manager and supervisor report date-filter checks without export.
- Advisor protected organization-route redirect.

## Generated artifacts

Playwright generated the following local, ignored outputs:

```text
playwright-report/
test-results/
```

Selected screenshots are stored below the relevant test-result directory for
local review. They must be redacted or replaced with synthetic data before
being copied into Bopadigital or the final report: the current seeded UI shows
real-looking names, email addresses, and business records.

## Limitations and follow-up at the time of this execution

- This is a local/manual sample; Playwright is not a required CI check.
- The suite uses Chromium only.
- API authentication is used for read-only journeys; UI login is exercised by
  the role smoke cases.
- Catalog/employability mutation journeys, document state transitions, file
  uploads, and reset/cleanup automation were intentionally outside this
  read-only execution. They are now covered by the separate
  [Phase 9 mutable execution](../phase-9-e2e-mutations/05-execution-2026-08-15.md).
- CSV export and CI orchestration remain outside the Phase 8/9 browser sample.
- The existing npm install reported dependency audit findings; no automated
  dependency upgrade or audit fix was performed as part of this phase.
